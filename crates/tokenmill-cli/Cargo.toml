[package]
name = "tokenmill-cli"
default-run = "tokenmill"
edition.workspace = true
license.workspace = true
version.workspace = true

[[bin]]
name = "tokenmill"
path = "src/main.rs"

[[bin]]
name = "tokenmill-tray"
path = "src/tray_launcher.rs"

[dependencies]
serde_json = "=1.0.145"
tokenmill-core = { path = "../tokenmill-core" }
tokenmill-acp = { path = "../tokenmill-acp" }

[lints]
workspace = true
